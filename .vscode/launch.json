{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch NovaPlay (HMR & HR)",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceRoot}/node_modules/.bin/vite",
      "windows": {
        "runtimeExecutable": "${workspaceRoot}/node_modules/.bin/vite.cmd"
      }
    }
  ]
}
